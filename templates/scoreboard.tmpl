{% extends "base.tmpl" %}

{% block content %}
		<h2>Users by distance traveled, this week</h2>
{% for item in distance_traveled_week %}
			<p><a href="/f/{{ item.0 }}">{{ item.0 }}</a> ({{ item.1|floatformat:2 }} kilometers) </p>
{% endfor %}
		<h2>Users by distance traveled, this month</h2>
{% for item in distance_traveled_month %}
			<p><a href="/f/{{ item.0 }}">{{ item.0 }}</a> ({{ item.1|floatformat:2 }} kilometers) </p>
{% endfor %}
		<h2>Users by distance traveled, all time</h2>
{% for item in distance_traveled_alltime %}
			<p><a href="/f/{{ item.0 }}">{{ item.0 }}</a> ({{ item.1|floatformat:2 }} kilometers) </p>
{% endfor %}
		<h2>Users by number of checkins, this week</h2>
{% for item in number_checkins_week %}
			<p><a href="/f/{{ item.0 }}">{{ item.0 }}</a> ({{ item.1 }} checkins) </p>
{% endfor %}
		<h2>Users by number of checkins, this month</h2>
{% for item in number_checkins_month %}
			<p><a href="/f/{{ item.0 }}">{{ item.0 }}</a> ({{ item.1 }} checkins) </p>
{% endfor %}
		<h2>Users by number of checkins, all time</h2>
{% for item in number_checkins_alltime %}
			<p><a href="/f/{{ item.0 }}">{{ item.0 }}</a> ({{ item.1 }} checkins) </p>
{% endfor %}
		<h2>Users by speed between checkins, this week</h2>
{% for item in checkin_speed_week %}
			<p><a href="/f/{{ item.0 }}">{{ item.0 }}</a> ({{ item.1|floatformat:5 }} kilometers/second) </p>
{% endfor %}
		<h2>Users by speed between checkins, this month</h2>
{% for item in checkin_speed_month %}
			<p><a href="/f/{{ item.0 }}">{{ item.0 }}</a> ({{ item.1|floatformat:5 }} kilometers/second) </p>
{% endfor %}
		<h2>Users by speed between checkins, all time</h2>
{% for item in checkin_speed_alltime %}
			<p><a href="/f/{{ item.0 }}">{{ item.0 }}</a> ({{ item.1|floatformat:5 }} kilometers/second) </p>
{% endfor %}
{% endblock %}